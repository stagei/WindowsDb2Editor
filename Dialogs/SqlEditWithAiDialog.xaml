<Window x:Class="WindowsDb2Editor.Dialogs.SqlEditWithAiDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Edit selected code (AI SQL)"
        Icon="pack://application:,,,/Resources/dEdge.ico"
        Width="700"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Edit selected code" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>

        <TextBox x:Name="InstructionBox" Grid.Row="1" MinHeight="60" MaxHeight="80" Margin="0,0,0,8"
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 ui:ControlHelper.PlaceholderText="e.g. Make sql statement return the 5 largest tablespaces in GB"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="EditSelectionButton" Content="Edit Selection" Width="120" Margin="0,0,8,0" Click="EditSelection_Click"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>

        <Border x:Name="ResultPanel" Grid.Row="3" Visibility="Collapsed" Margin="0,8,0,0" Padding="8" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Original (red)" FontWeight="SemiBold" Foreground="#E0A0A0" Margin="0,0,0,4"/>
                <TextBox x:Name="OldSqlBox" Grid.Row="1" IsReadOnly="True" MinHeight="80" MaxHeight="120"
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         Background="#2D1515" Foreground="#E0A0A0" Padding="6" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="2" Text="Modified (green)" FontWeight="SemiBold" Foreground="#A0E0A0" Margin="0,0,0,4"/>
                <TextBox x:Name="NewSqlBox" Grid.Row="3" IsReadOnly="True" MinHeight="80" MaxHeight="120"
                         TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                         Background="#152D15" Foreground="#A0E0A0" Padding="6" Margin="0,0,0,8"/>
                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="RejectButton" Content="Reject" Width="80" Margin="0,0,8,0" Click="Reject_Click"/>
                    <Button x:Name="AcceptButton" Content="Accept" Width="80" Click="Accept_Click" IsDefault="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="4" Margin="0,8,0,0" Foreground="Gray" TextWrapping="Wrap" Visibility="Collapsed"/>
    </Grid>
</Window>
