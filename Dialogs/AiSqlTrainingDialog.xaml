<Window x:Class="WindowsDb2Editor.Dialogs.AiSqlTrainingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="AI SQL Training"
        Icon="pack://application:,,,/Resources/dEdge.ico"
        Width="750"
        Height="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Scenario" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <ComboBox x:Name="ScenarioCombo" Grid.Row="1" Margin="0,0,0,8" SelectionChanged="ScenarioCombo_SelectionChanged"/>

        <Button x:Name="RunButton" Grid.Row="2" Content="Run" Width="80" HorizontalAlignment="Left" Margin="0,0,0,8" Click="Run_Click"/>

        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Original SQL" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,4"/>
            <TextBox x:Name="OriginalSqlBox" Grid.Row="1" IsReadOnly="True" MinHeight="60" Margin="0,0,0,8"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="6"/>
            <TextBlock Grid.Row="2" Text="Instruction" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,4"/>
            <TextBox x:Name="InstructionBox" Grid.Row="3" IsReadOnly="True" MinHeight="40" Margin="0,0,0,8"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="6"/>
            <TextBlock Grid.Row="4" Text="LLM output" FontWeight="SemiBold" Foreground="Gray" Margin="0,0,0,4"/>
            <TextBox x:Name="OutputBox" Grid.Row="5" IsReadOnly="True" MinHeight="80" Margin="0,0,0,0"
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="6"/>
        </Grid>

        <TextBlock x:Name="PassFailText" Grid.Row="4" Margin="0,8,0,4" FontWeight="SemiBold" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Close" Width="80" Click="Close_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
