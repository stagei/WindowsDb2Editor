<Window x:Class="WindowsDb2Editor.Dialogs.OllamaModelDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Download SQL model"
        Width="420"
        Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="MessageText" Grid.Row="0" Text="Download recommended SQL model (defog-llama3-sqlcoder-8b, ~4.7GB)?" 
                   TextWrapping="Wrap" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DownloadButton" Content="Download" Width="90" Margin="0,0,8,0" Click="Download_Click" IsDefault="True"/>
            <Button Content="Skip" Width="70" Click="Skip_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
