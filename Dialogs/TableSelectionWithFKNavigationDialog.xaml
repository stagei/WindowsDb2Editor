<Window x:Class="WindowsDb2Editor.Dialogs.TableSelectionWithFKNavigationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Table Selection - Add Tables to Mermaid Model"
        Icon="pack://application:,,,/Resources/dEdge.ico"
        Width="750"
        Height="700"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <!-- Main Content Grid -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Status Text -->
            <TextBlock Grid.Row="2" x:Name="StatusText" Text="Loading tables..." Margin="0,0,0,8" 
                       FontSize="13" Foreground="#AAAAAA"/>
            
            <!-- Search Box -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="SearchTextBox" ui:ControlHelper.PlaceholderText="Search tables..." Width="450" TextChanged="SearchTextBox_TextChanged"/>
            </StackPanel>
            
            <!-- Schema and Filter -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Schema:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="SchemaComboBox" Width="180" Margin="0,0,20,0" SelectionChanged="SchemaComboBox_SelectionChanged"/>
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="FilterComboBox" Width="150" SelectedIndex="0" SelectionChanged="FilterComboBox_SelectionChanged">
                    <ComboBoxItem Content="All Tables"/>
                    <ComboBoxItem Content="With FKs"/>
                    <ComboBoxItem Content="Referenced"/>
                </ComboBox>
            </StackPanel>
            
            <!-- Table List (Simple Checkbox List) -->
            <Border Grid.Row="3" BorderBrush="{DynamicResource SystemControlForegroundBaseMediumBrush}" 
                    BorderThickness="1" Padding="5" Margin="0,0,0,10">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="320">
                    <ItemsControl x:Name="TablesList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="5,4">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding TableName}" FontWeight="SemiBold" FontSize="13" Margin="0,0,12,0"/>
                                        <TextBlock Foreground="#CCCCCC" FontSize="12" VerticalAlignment="Center">
                                            <Run Text="["/>
                                            <Run Text="{Binding OutgoingFKCount}" Foreground="#4FC3F7"/>
                                            <Run Text=" out, "/>
                                            <Run Text="{Binding IncomingFKCount}" Foreground="#81C784"/>
                                            <Run Text=" in]"/>
                                        </TextBlock>
                                    </StackPanel>
                                </CheckBox>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
            
            <!-- Selection Count -->
            <TextBlock Grid.Row="4" x:Name="SelectionCountText" Margin="0,0,0,10" FontSize="13" Foreground="#AAAAAA"/>
        
        <!-- FK Navigation Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <Button x:Name="FollowFKButton" Content="Follow Foreign Keys →" Click="FollowFK_Click" Padding="10,5" Margin="0,0,10,0" ToolTip="Add tables referenced by selected tables"/>
            <Button x:Name="FollowIncomingFKButton" Content="← Follow Incoming FKs" Click="FollowIncomingFK_Click" Padding="10,5" Margin="0,0,10,0" ToolTip="Add tables that reference selected tables"/>
            <Button x:Name="ExpandAllRelatedButton" Content="Expand All Related" Click="ExpandAllRelated_Click" Padding="10,5" Margin="0,0,10,0" ToolTip="Recursively add all tables connected via FK relationships"/>
            <Button Content="Clear Selection" Click="ClearSelection_Click" Padding="10,5"/>
        </StackPanel>
        
        <!-- FK Relationships Panel -->
        <GroupBox Grid.Row="6" Header="Foreign Key Relationships" Margin="0,0,0,10">
            <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="FKRelationshipsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Margin="5,2" FontSize="12">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="• {0}">
                                        <Binding/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add to Model" Click="AddToModel_Click" IsDefault="True" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Cancel" IsCancel="True" Padding="15,8"/>
        </StackPanel>
        </Grid>
        
        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Background="#CC1E1E1E">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:ProgressRing IsActive="True" Width="60" Height="60"/>
                <TextBlock x:Name="LoadingText" Text="Loading tables..." FontSize="16" 
                           Foreground="White" Margin="0,15,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
