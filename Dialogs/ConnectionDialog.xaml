<Window x:Class="WindowsDb2Editor.Dialogs.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="New DB2 Connection"
        Height="400"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Connection Name -->
        <TextBlock Grid.Row="0" Text="Connection Name:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Margin="0,0,0,15"/>

        <!-- Server -->
        <TextBlock Grid.Row="2" Text="Server:" Margin="0,0,0,5"/>
        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="ServerTextBox" Margin="0,0,10,0"/>
            <TextBlock Grid.Column="1" Text="Port:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="2" x:Name="PortTextBox" Text="50000"/>
        </Grid>

        <!-- Database -->
        <TextBlock Grid.Row="4" Text="Database:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="5" x:Name="DatabaseTextBox" Margin="0,0,0,15"/>

        <!-- Username -->
        <TextBlock Grid.Row="6" Text="Username:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="7" x:Name="UsernameTextBox" Margin="0,0,0,15" VerticalAlignment="Top"/>

        <!-- Password -->
        <TextBlock Grid.Row="8" Text="Password:" Margin="0,0,0,5" VerticalAlignment="Top"/>
        <PasswordBox Grid.Row="9" x:Name="PasswordBox" Margin="0,0,0,0" VerticalAlignment="Top"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Test Connection" Width="120" Margin="0,0,10,0" Click="TestConnection_Click"/>
            <Button Content="Connect" Width="100" Margin="0,0,10,0" IsDefault="True" Click="Connect_Click"/>
            <Button Content="Cancel" Width="100" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>

