make sure to add " AS <database provider agnostic name>" to columns to all statements in the db2 sqlstatement json files, and update all usages of the query column to use this new database agnostic name, to support additional database providers in the future. This also applies to all classes that currently use db2 specific naming on property elements