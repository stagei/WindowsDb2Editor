<Window x:Class="WindowsDb2Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowsDb2Editor"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        mc:Ignorable="d"
        Title="WindowsDb2Editor - DB2 Database Manager" 
        Height="800" 
        Width="1200"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar - Word-inspired modern design -->
        <Menu Grid.Row="0" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
            <MenuItem Header="_File" Padding="12,8">
                <MenuItem Header="_New Connection" Click="NewConnection_Click" InputGestureText="Ctrl+N">
                    <MenuItem.Icon>
                        <TextBlock Text="➕" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="RecentConnectionsMenuItem" Header="_Recent Connections">
                    <MenuItem.Icon>
                        <TextBlock Text="🕐" FontSize="16"/>
                    </MenuItem.Icon>
                    <!-- Recent connections will be populated dynamically -->
                    <MenuItem Header="(No recent connections)" IsEnabled="False"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Close Tab" Click="CloseTab_Click" InputGestureText="Ctrl+W">
                    <MenuItem.Icon>
                        <TextBlock Text="✖" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click" InputGestureText="Alt+F4">
                    <MenuItem.Icon>
                        <TextBlock Text="🚪" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_View" Padding="12,8">
                <MenuItem Header="_Dark Theme" Click="ToggleDarkMode_Click" InputGestureText="Ctrl+D">
                    <MenuItem.Icon>
                        <TextBlock Text="🌙" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Cycle _Theme (Dark/Light/System)" ToolTip="Press Ctrl+D to cycle through themes"/>
                <Separator/>
                <MenuItem Header="Query _History..." Click="QueryHistory_Click" InputGestureText="Ctrl+H">
                    <MenuItem.Icon>
                        <TextBlock Text="📜" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Settings..." Click="Settings_Click">
                    <MenuItem.Icon>
                        <TextBlock Text="⚙" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Help" Padding="12,8">
                <MenuItem Header="_About" Click="About_Click">
                    <MenuItem.Icon>
                        <TextBlock Text="ℹ" FontSize="16"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>

        <!-- Main content area -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Tab Control for Connections -->
            <TabControl Grid.Row="0" x:Name="ConnectionTabs" Margin="5" SelectionChanged="ConnectionTabs_SelectionChanged">
                <!-- Tabs will be added dynamically -->
            </TabControl>

            <!-- Status Bar -->
            <StatusBar Grid.Row="0" x:Name="MainStatusBar" Height="25" VerticalAlignment="Bottom" 
                       Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}" Visibility="Collapsed">
                <StatusBarItem>
                    <TextBlock x:Name="StatusBarText" Text="Ready"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock x:Name="ConnectionCountText" Text="0 connections"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>

        <!-- Placeholder when no tabs - Word-inspired welcome screen -->
        <Grid Grid.Row="1" x:Name="PlaceholderGrid" Visibility="Visible" 
              Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="600">
                <!-- App Icon/Title -->
                <TextBlock Text="WindowsDb2Editor" 
                          FontSize="32" 
                          FontWeight="Bold" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,10"
                          Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                
                <TextBlock Text="Modern DB2 Database Manager for Windows 11" 
                          FontSize="14" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,40"
                          Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                
                <!-- Quick Start Card -->
                <Border Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
                        CornerRadius="8"
                        Padding="30"
                        BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Get Started" 
                                  FontSize="18" 
                                  FontWeight="SemiBold" 
                                  Margin="0,0,0,20"
                                  Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
                        
                        <Button Content="➕  New Connection" 
                                Click="NewConnection_Click" 
                                Padding="20,12" 
                                FontSize="14"
                                MinWidth="200"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,10"/>
                        
                        <TextBlock Text="Ctrl+N" 
                                  FontSize="12" 
                                  HorizontalAlignment="Center"
                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                    </StackPanel>
                </Border>
                
                <!-- Features List -->
                <StackPanel Margin="0,30,0,0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel Margin="0,0,30,0">
                        <TextBlock Text="✓" FontSize="20" HorizontalAlignment="Center" Foreground="Green"/>
                        <TextBlock Text="Multiple Tabs" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,30,0">
                        <TextBlock Text="✓" FontSize="20" HorizontalAlignment="Center" Foreground="Green"/>
                        <TextBlock Text="SQL Formatting" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="✓" FontSize="20" HorizontalAlignment="Center" Foreground="Green"/>
                        <TextBlock Text="Dark/Light Theme" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
